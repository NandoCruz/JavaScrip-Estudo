function verificar() {
    var data = new Date()
    var ano = data.getFullYear()
    var nome = window.document.getElementById("nome")
    var fano = window.document.getElementById("ano")
    lert = (`Olá, ${nome}! você nasceu no ano de ${fano}`)
}